import { Typography, Box } from '@mui/material';

export default function App() {
  return (
    <Box display="flex" justifyContent="center" alignItems="center" minHeight="100vh">
      <Typography variant="h4">EMD AR Backoffice</Typography>
    </Box>
  );
}
